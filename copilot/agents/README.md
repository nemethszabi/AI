# `copilot\agents\` — intentionally empty

No `.agent.md` files here yet. This is the Copilot CLI branch's doctrine-only tier — see `..\README.md`
("Why doctrine-only, for now") for the reasoning. Would hold `.agent.md` twins of `..\..\claude\agents\
*.md` if a port of the `req-*`/`dev-*` families is ever approved; see `..\AGENT-TEMPLATE-BASELINE.md` for
the frontmatter shape a port would use.
